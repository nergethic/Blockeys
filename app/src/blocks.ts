//import './main.scss';

//class Socket {
    //block1, block2
//}

/*
class Block {
    constructor() {

    }
}

class AddBlock extends Block {
    inputValueA: number = 0;
    inputValueB: number = 0;
    outputValue: number;

    inputBlockA: Block;
    inputBlockB: Block;
    outputBlock: Block;

    outputType: "number";

    inputAIsConnected: boolean;
    inputBIsConnected: boolean;

    OnConnectTry() {
        let result = this.Validate();
    }

    Validate(): boolean {
        //if (outputBlock.)

        return true;
    }

    UpdateInputs() {
        if (this.inputBlockAConnected)
            this.inputValueA = this.inputBlockA.outputValue;
         
        if (this.inputBlockBConnected)
            this.inputValueB = this.inputBlockB.outputValue;
    }

    Update() {
        this.UpdateInputs();
        this.outputValue = this.inputValueA + this.inputValueB;
        this.outputBlock.Update();
    }
}

function App() {

    let block1 = new AddBlock();
    let block2 = new AddBlock();

    let success: boolean = TryToMakeConnection(block1.outputs[0], block2.inputs[0]);
    if (!success) {
        alert("error");
    }

    block1.Update();
    console.log("result: " + block1.outputValue);

    while (true) {
        //firstBlock.Update();
    }
}
*/